import 'package:flutter/material.dart';

class AppMessageDetail extends StatelessWidget {
  //const AppMessageDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        //appBar:,
        );
  }
}
